/**
 * ============================================================================
 * 会员开通 —— 已并进「我的」页底部抽屉
 * ============================================================================
 *
 * 不要再从「我的」navigateTo 这一页。若被旧入口打开，退回上一页。
 */
import { navigateBackOrHome } from '../../utils/navigate';

Page({
  onLoad() {
    navigateBackOrHome();
  },
});
