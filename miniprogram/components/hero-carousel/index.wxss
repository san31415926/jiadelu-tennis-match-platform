/**
 * ============================================================================
 * 头部轮播样式
 * ============================================================================
 * 渐变背景、波浪、大标题的样式在 app.wxss 里（七个页面共用），
 * 这个文件只负责轮播文案和指示点。数值来自 Figma node 1:229。
 */

/**
 * 轮播区域。
 *
 * 绝对定位在 top: 94rpx，也就是大标题下方。高度 76rpx 刚好容纳
 * 主标题（46rpx 行高）+ 副标题（26rpx 行高）。
 *
 * 如果轮播文案要加第三行，把 height 调大，但要注意别撞到 top: 192rpx
 * 位置的指示点。
 *
 * 自动播放间隔、是否循环这些行为参数在 index.wxml 的 swiper 标签上：
 *   interval="5000" 是 5 秒切一张，改成 3000 会更快
 *   duration="400"  是切换动画时长
 *   删掉 autoplay 就不自动播放，只能手动滑
 */
.banner {
  position: absolute;
  left: 0;
  top: 94rpx;
  width: 100%;
  height: 76rpx;
}

/** 每一张的内容容器。左内边距 52rpx 是设计稿给的左边距 */
.banner__slide {
  padding-left: 52rpx;
}

/** 轮播主标题，如「广佛俱乐部联名赛」 */
.banner__title {
  font-size: 36rpx;
  font-weight: 700;
  line-height: 46rpx;
  color: var(--text-banner-title);
}

/** 轮播副标题，如「球员精彩瞬间 · 点击查看」 */
.banner__subtitle {
  font-size: 19rpx;
  line-height: 26rpx;
  color: var(--text-banner-sub);
}

/**
 * ----------------------------------------------------------------------------
 * 轮播指示点
 * ----------------------------------------------------------------------------
 *
 * 【为什么三个点大小不一样】
 * 设计稿（Figma node 1:235）里三个点分别是 14、12、10 直径，还带一点垂直
 * 偏移，做出一种"往右后方延伸"的透视感。它是固定的装饰效果，不随当前页
 * 变化——也就是说切到第二张时，是第二个点变深色，但它仍然是 12rpx 的中号点。
 *
 * 【想改成常见的等大圆点】
 * 把下面三个 --1/--2/--3 的 width/height 都改成同一个值（比如 12rpx），
 * top 都改成 1rpx，left 按等间距重排（0 / 22 / 44）。
 *
 * 【超过三张轮播怎么办】
 * 现在只定义了三个位置类。如果轮播有四张以上，第四个点会没有样式而消失。
 * 需要在这里补 .banner__dot--4 等规则，或者改成上面说的等大等距方案，
 * 用一个通用类 + flex 布局自动排列。
 */
.banner__dots {
  position: absolute;
  left: 55rpx;
  top: 192rpx;
  width: 58rpx;
  height: 14rpx;
}

.banner__dot {
  position: absolute;
  border-radius: 50%;
  background: var(--dot-idle);
  /* 切换时颜色渐变过渡，去掉这行会变成生硬的瞬间切换 */
  transition: background-color 0.2s;
}

.banner__dot--1 {
  left: 0;
  top: 0;
  width: 14rpx;
  height: 14rpx;
}

.banner__dot--2 {
  left: 25rpx;
  top: 2rpx;
  width: 12rpx;
  height: 12rpx;
}

.banner__dot--3 {
  left: 48rpx;
  top: 4rpx;
  width: 10rpx;
  height: 10rpx;
}

/** 当前那一页对应的点，颜色变深 */
.banner__dot.is-active {
  background: var(--dot-active);
}
