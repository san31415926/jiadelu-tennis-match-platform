---
description: 从 Figma 还原界面的工作流与尺寸换算规则
alwaysApply: true
---

# Figma 还原规则

设计稿是唯一标准。文件 key `nzKWauHC37fyjpAtA3rjfd`（「网球赛事总结」），
用 Figma 远程 MCP 读取（`plugin-figma-figma`）。

## 换算规则

**设计稿 1px = 1rpx，不做任何换算。** 画板宽 750px，小程序 750rpx = 屏幕宽，正好对应。
看到代码里有 `* 1.15` 之类的系数就是错的。

画板高 1184px **不代表屏幕高度**，真机可视高度更大。页面按「头部固定高度 +
内容区流式」实现，不要按画板总高等比缩放，也不要给页面设固定总高。

## 读取设计稿前先加载 skill

调 `get_design_context` 前必须先读 `figma-design-to-code` skill（工具描述里强制要求）。
返回的 React + Tailwind 代码是**参考**，要改写成 WXML/WXSS，不要直接抄。

节点很大时会返回稀疏 metadata，此时改为分区域读取子节点，不要硬取整页。

## 同一页面的多个画板通常只是状态不同

三版首页只是筛选选中态不同，两版榜单只是城市榜/全国榜选中态相反。
**要收敛成一个页面的动态状态**，不要各写一遍。读之前先用 `get_screenshot` 对比确认。

## 资产一律用 Figma 导出的原始文件

不要从设计稿 PNG 上裁切，也不要自己画近似图形。已经踩过的坑：

- 底栏图标未选中态在设计里是 `opacity: 52%`，自己切图会把褪色效果烘进图片
- 图标素材板上图标的白色和背景是同一个色值 `rgb(247,247,246)`，算法上无法分离

导出后用 `tools/build-figma-assets.cjs` 压缩落地，**每个资产按自己标注的尺寸单独设定
宽高**，不要套用统一尺寸（七个宫格图标的尺寸在设计里各不相同）。

## 目视取值要标注出来

绝大多数数值来自节点标注。个别设计稿未导出的状态（如日历非本月日期的浅灰色）
是从 `测试图/` 采样或目视的，注释里必须写明，便于日后校准。
