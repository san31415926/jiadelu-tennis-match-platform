/**
 * ============================================================================
 * 云开发环境配置 —— 假数据与真实数据的总开关
 * ============================================================================
 *
 * 【这个文件是干什么的】
 * 决定小程序从哪里取数据。填了环境 ID，app.ts 会初始化云能力。
 * 每一块业务还有自己的 USE_MOCK 开关：没接完的模块继续走 mock/，
 * 接完的改 false 就改走云函数 / 云数据库。
 *
 * 【怎么拿到环境 ID】
 * 1. 微信开发者工具打开【仓库根目录】（不要只打开 miniprogram/）
 * 2. 点顶部「云开发」，在「设置 - 环境设置」里复制环境 ID
 * 3. 填到下面 CLOUD_ENV_ID 的引号里
 *
 * 【模块开关怎么用】
 * 某一块接上云、控制台里也能看到数据了，把对应项改成 false。
 * 现在登录、赛事、俱乐部、榜单、相册、店铺、参赛记录都已接云。
 *
 * 【注意】
 * 云开发要求基础库 2.2.3 以上。个人主体可以登录和写库，
 * 但不能接微信支付（报名收费需要企业主体）。
 */

/** 云开发环境 ID。留空 = 不初始化云 */
export const CLOUD_ENV_ID = 'cloud1-d9go68a317d35c262';

/**
 * 按模块决定走假数据还是云。
 * true = 仍读 mock/；false = 调云函数 / 云数据库。
 * 改这里之前，确认对应云函数已经在开发者工具里上传成功。
 */
export const USE_MOCK = {
  /** 「我的」登录、资料保存 */
  auth: false,
  /** 首页 / 超级杯 / 详情 / 日历 / 报名 */
  events: false,
  /** 俱乐部列表 / 主页 / 相册 / 店铺 / 参赛记录 */
  clubs: false,
  /** 球员榜 / 俱乐部榜 */
  ranking: false,
};

/** 兼容旧写法。有环境 ID 且 auth 或 events 任一走云，就视为不再全站假数据 */
export const USE_MOCK_DATA = !CLOUD_ENV_ID;
