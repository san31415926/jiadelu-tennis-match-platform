/**
 * ============================================================================
 * 会员开通页样式 —— 视觉刷新草稿 V5
 * ============================================================================
 *
 * 数值来自 Figma node 355:361（Sheet / Checkout）。
 * 真机里这是独立一页，不是压在「我的」上的抽屉，所以加了吸顶返回。
 * 设计稿 1px = 1rpx。
 *
 * 想改开通按钮颜色 → 改 .checkout__btn 的 background
 * 想改选中套餐描边 → 改 .plan.is-active 的 border-color
 */

.membership {
  min-height: 100vh;
  background: var(--page-bg);
}

.sheet {
  padding: 16rpx 0 24rpx;
}

.sheet__handle {
  width: 77rpx;
  height: 8rpx;
  margin: 0 auto 16rpx;
  border-radius: 4rpx;
  background: #d8d8d4;
}

.sheet__kicker {
  padding: 0 32rpx;
  font-size: 22rpx;
  line-height: 26rpx;
  color: #73786b;
}

.plans {
  width: 100%;
  margin-top: 24rpx;
  white-space: nowrap;
}

.plan {
  position: relative;
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  width: 200rpx;
  height: 248rpx;
  margin-left: 12rpx;
  padding: 28rpx 12rpx 20rpx;
  overflow: hidden;
  vertical-align: top;
  border: 2rpx solid #e8e8e5;
  border-radius: 20rpx;
  background: #ffffff;
}

.plan:first-child {
  margin-left: 32rpx;
}

.plan:last-child {
  margin-right: 32rpx;
}

.plan.is-active {
  border-color: var(--accent);
  background: #f7fbe8;
}

.plan__flag {
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  padding: 4rpx 10rpx;
  border-radius: 0 0 8rpx 8rpx;
  background: var(--accent);
  font-size: 16rpx;
  font-weight: 700;
  line-height: 19rpx;
  color: #1a1f16;
}

.plan__name {
  margin-top: 36rpx;
  font-size: 24rpx;
  font-weight: 700;
  line-height: 29rpx;
  color: #1f211c;
}

.plan__badge {
  margin-top: 10rpx;
  padding: 3rpx 10rpx;
  border-radius: 6rpx;
  background: #f3f3f1;
  font-size: 16rpx;
  line-height: 19rpx;
  color: #73786b;
}

.plan.is-active .plan__badge {
  background: rgba(131, 212, 20, 0.22);
  color: #3a6b08;
}

.plan__price {
  display: flex;
  align-items: flex-end;
  margin-top: 10rpx;
  color: #1f211c;
}

.plan.is-active .plan__price {
  color: #1a1f16;
}

.plan__yen {
  margin-bottom: 6rpx;
  font-size: 24rpx;
  font-weight: 700;
}

.plan__num {
  font-size: 40rpx;
  font-weight: 800;
  line-height: 48rpx;
}

.plan__origin {
  margin-top: 10rpx;
  font-size: 18rpx;
  line-height: 22rpx;
  color: #9b9e96;
  text-decoration: line-through;
}

.hint {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 24rpx;
  padding: 0 32rpx;
  font-size: 20rpx;
  line-height: 24rpx;
  color: #73786b;
}

.hint__q {
  width: 26rpx;
  height: 26rpx;
  border: 1rpx solid #c8cbc3;
  border-radius: 50%;
  font-size: 18rpx;
  line-height: 24rpx;
  color: #9b9e96;
  text-align: center;
}

.pay-title {
  margin-top: 16rpx;
  padding: 8rpx 32rpx;
  font-size: 26rpx;
  font-weight: 700;
  line-height: 31rpx;
  color: #1f211c;
}

.pays {
  display: flex;
  gap: 12rpx;
  padding: 0 32rpx;
}

.pay {
  display: flex;
  flex: 1;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
  height: 68rpx;
  border: 2rpx solid #e8e8e5;
  border-radius: 16rpx;
  background: #ffffff;
  font-size: 22rpx;
  color: #1f211c;
}

.pay.is-active {
  border-color: var(--accent);
  background: #f7fbe8;
}

.pay__mark {
  width: 36rpx;
  height: 36rpx;
  border-radius: 8rpx;
  background: #07c160;
  font-size: 18rpx;
  font-weight: 700;
  line-height: 36rpx;
  color: #ffffff;
  text-align: center;
}

.pay:last-child .pay__mark {
  background: #1677ff;
}

.checkout-spacer {
  height: calc(200rpx + env(safe-area-inset-bottom));
}

.checkout {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 10;
  padding: 20rpx 32rpx calc(20rpx + env(safe-area-inset-bottom));
  background: #ffffff;
}

.checkout__btn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  height: 80rpx;
  border-radius: 16rpx;
  background: var(--accent);
}

.checkout__label {
  font-size: 30rpx;
  font-weight: 700;
  color: inherit;
}

.checkout__save {
  font-size: 20rpx;
  color: inherit;
  opacity: 0.78;
}

.checkout__agree {
  margin-top: 12rpx;
  font-size: 18rpx;
  line-height: 22rpx;
  color: #9b9e96;
  text-align: center;
}
