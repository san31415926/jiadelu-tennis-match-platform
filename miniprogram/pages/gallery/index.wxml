<view class="gallery theme-{{theme}}" style="{{themeVars}}">
  <page-nav title="相册" occupy="{{true}}" />
  <view class="kicker">{{summary}}</view>

  <!-- 分类筛选，Figma node 23:281 -->
  <view class="filters">
    <view
      class="filters__chip {{activeFilter === item ? 'is-active' : ''}}"
      style="{{activeFilter === item ? 'background:' + accent + ';color:' + accentText + ';border-color:' + accent : ''}}"
      wx:for="{{filters}}"
      wx:key="*this"
      data-filter="{{item}}"
      bindtap="onFilterTap"
    >
      {{item}}
    </view>
  </view>

  <!-- 分组列表，Figma node 23:292 / 23:300 / 23:328 -->
  <view
    class="section {{item.layout === 'bracket' ? 'section--bracket' : ''}}"
    wx:for="{{sections}}"
    wx:key="id"
  >
    <view class="section__heading">
      <view class="section__title">
        <text
          wx:for="{{item.titleParts}}"
          wx:for-item="part"
          wx:key="text"
          class="section__title-part {{part.brand ? 'section__title-part--brand' : ''}}"
        >{{part.text}}</text>
      </view>
      <view class="section__more" data-id="{{item.id}}" bindtap="onViewAll">查看全部 ›</view>
    </view>

    <view class="section__subtitle" wx:if="{{item.subtitle}}">{{item.subtitle}}</view>

    <view class="section__photos">
      <image
        class="section__photo"
        wx:for="{{item.photos}}"
        wx:for-item="photo"
        wx:key="*this"
        src="{{photo}}"
        mode="aspectFill"
        data-src="{{photo}}"
        data-group="{{item.id}}"
        bindtap="onPreview"
      />
    </view>

    <view class="section__count">{{item.count}}</view>
    <view class="section__divider" wx:if="{{item.layout === 'photo'}}"></view>
  </view>
  <view class="feed__empty" wx:if="{{sections.length === 0}}">该分类下暂无照片</view>
</view>
