# 项目上下文（给接手的 AI）

嘉得路网球对战平台的微信小程序端。当前阶段：**从 Figma 定稿设计稿还原界面**，
数据全部是假数据，还没接后端。

## 必读顺序

1. 本文件 —— 项目全貌与硬约束
2. `功能清单.md` —— 开发范围。**对战/约战匹配本期不做**，不要主动实现
3. `miniprogram/工程说明.md` —— 目录结构、`.json` 配置逐项说明、改动速查表、容易踩的坑
4. `README.md` —— 环境准备、已还原页面清单
5. `待办清单.md` —— 还缺哪些页、哪些功能仍是假的

`.cursor/rules/` 下有四条规则，分别管注释风格、小程序耦合约束、Figma 还原、资产流水线。

## 技术栈

原生微信小程序 + TypeScript，无框架无构建步骤（TS 由开发者工具的编译插件处理）。
后端计划用微信云开发，环境 ID 填在 `miniprogram/config/env.ts`，留空则全站走
`miniprogram/mock/` 的假数据。

**不要引入 npm 依赖、构建工具或 CSS 框架**。这个项目刻意保持零构建。

## 硬约束

**设计稿 1px = 1rpx。** Figma 画板宽 750px，小程序 750rpx = 屏幕宽，所以设计稿上量到的
数值直接写，不做任何换算。看到有人写 `* 1.5` 之类的换算就是错的。

**画板高度不代表屏幕高度。** 画板 1184px 短于真机可视高度，页面必须按
「头部固定高度 + 内容区流式」实现，不要按画板总高等比缩放。

**自定义导航栏与底栏。** `app.json` 里 `navigationStyle: "custom"` 且
`tabBar.custom: true`。所以每个页面头部要用 `padding-top:{{statusBarHeight}}px`
避开状态栏，每个 Tab 页要在 `onShow` 调 `syncTabBarSelected`。

**主包上限 2MB。** 当前 `miniprogram/assets/` 约 860KB。新增图片前先想能不能用
CSS 或内联 SVG 实现。

## 设计稿

Figma 文件 key `nzKWauHC37fyjpAtA3rjfd`（「网球赛事总结」），通过 Figma 远程 MCP 读取。
七个已还原页面对应的节点号写在各页面 wxss 的头部注释里。

`测试图/` 是设计稿的 PNG 导出，作为视觉验收基准。同一页面常有多个画板，
往往只是状态不同（如三版首页只是筛选选中态不同），**不要当成不同页面各写一遍**。

## 沟通语言

项目所有文档、注释、提交信息都用中文。回复用户也用中文。

## 已知技术债

1. 领奖台金圈仍烤在台座切图里；「我的」页已拆成金圈 + 头像 + 相机三层，换真实头像只改 `profile.avatar`
2. 榜单头像、俱乐部 logo、球场照片是本地示例图，应改为云存储网络图
3. 设计稿正文字号偏小（卡片信息 17rpx ≈ 8.5pt），已按设计实现，等真机验证后再定是否放大
4. 尚无终稿画板的页面：赛事详情、参赛记录。赛事详情草稿在 Figma 草稿 `447:103`，点卡片仍 toast；参赛记录入口不在 `utils/navigate.ts` 白名单。其余见 `待办清单.md`
