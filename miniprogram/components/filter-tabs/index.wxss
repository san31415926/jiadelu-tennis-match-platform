/**
 * ============================================================================
 * 状态筛选条样式（首页 / 超级杯共用；V5 走 capsule）
 * ============================================================================
 * 数值来自 Figma node 1:261 / 1:367。
 */

/**
 * 筛选条容器。
 *
 * 【为什么右边是空的】
 * 四项各 174.5rpx，合计 698rpx，屏幕是 750rpx，所以右侧会空出 52rpx。
 * 这是设计稿的原始布局（四项整体左对齐），不是算错。
 *
 * 想让四项均分整个屏幕宽度：把下面 .filters__item 的 width 改成 flex: 1，
 * 并去掉固定 width。
 */
.filters {
  display: flex;
  height: 46rpx;
  background: var(--card-bg);
}

/**
 * 单个筛选项。
 *
 * width: 174.5rpx 决定了每项最多放 4 个汉字（18rpx 字号下）。
 * 如果筛选项名字要用 5 个字以上，把这里调宽，或改成 flex: 1 自适应。
 *
 * border-radius 用 --radius-tab（23rpx），正好是高度 46rpx 的一半，
 * 所以选中态是完美的药丸形。改高度要同步改圆角。
 *
 * 注意：未选中项也有 border-radius，只是没有背景色所以看不出来。
 * 这样切换时不会有圆角突变的跳动感。
 */
.filters__item {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 174.5rpx;
  height: 46rpx;
  border-radius: var(--radius-tab);
  font-size: 22rpx;
  color: var(--text-muted);
}

/**
 * 选中态。
 *
 * 【背景色不在这里】
 * 背景色由页面通过 active-color 属性传入，在 wxml 里以内联样式生效。
 * 这里只负责文字加粗和变深。
 */
.filters__item.is-active {
  font-weight: 600;
  color: var(--text-tab-active);
}

/**
 * V5 胶囊条（Figma 148:199）。四项均分，选中态白字。
 *
 * 设计稿未选中是浅灰底浅灰字，叠在换过的页底上会糊成一块。
 * 外框改成白底 + 深一点描边，未选中字加深，选中项加一点投影，
 * 当前项才一眼能认出来。想收回原稿对比，把 border / 未选中 color
 * 改回 #e5e5e0 和 #73786b，并去掉 box-shadow。
 */
.filters--capsule {
  gap: 8rpx;
  height: auto;
  padding: 10rpx;
  border: 2rpx solid #cfd2c6;
  border-radius: 999rpx;
  background: #ffffff;
}

.filters--capsule .filters__item {
  flex: 1;
  width: auto;
  height: auto;
  padding: 14rpx 8rpx;
  font-size: 22rpx;
  font-weight: 600;
  line-height: 30rpx;
  letter-spacing: 0.44rpx;
  color: #3d4436;
  background: #e8e8e2;
}

.filters--capsule .filters__item.is-active {
  font-weight: 700;
  box-shadow: 0 4rpx 10rpx rgba(20, 24, 16, 0.18);
}
