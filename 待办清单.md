# 待办清单

> 2026-08-27 对照功能清单与当前小程序整理。
> **对战 / 约战匹配本期不做**，不要主动实现。
> 登录、赛事、报名、俱乐部、榜单、相册、店铺、参赛记录已接到云开发（需在开发者工具上传云函数）。

---

## 现在就能接着做（界面）

| 优先级 | 事项 | 现状 | 建议 |
| --- | --- | --- | --- |
| P1 | **年会典礼 / 历届冠军** | 已接到海报页（黄条 + 一张图），功能清单里的奖项、着装、分项目冠军榜、未夺冠 `?` 占位还没有独立版式 | 有新画板再升级；没画板就维持海报 |
| P2 | **商务合作 / 关于我们二维码** | 虚线占位 | 换成真实客服 / 公众号图 |

---

## 看起来像功能、其实还是假的

点了只会 toast 或写本地示例，要等云开发或微信能力：

| 事项 | 现在会怎样 |
| --- | --- |
| 微信登录 | 「我的」点登录调 `login` 云函数，在 `users` 里建/查当前微信用户 |
| 赛事报名 / 立即报名 | 进报名页；去支付写入 `registrations`，toast「报名已提交（支付待开通）」 |
| 申请加入俱乐部 / 创建俱乐部 | 调 `clubAction`，写入 `club_members` 并更新 `users.club` |
| 我的资料保存 | 调 `updateProfile` 写入 `users` |
| 开通会员购买 | 「购买待接入支付」 |
| 联系客服 | 页上有 `open-type="contact"`，公众平台未开通客服时会失败 |
| 赛事组队招募 | 发布/加入只改本页示例或 toast，不是约球匹配 |

---

## 接后端之后

1. ~~填写 `miniprogram/config/env.ts` 的云开发环境 ID。~~ 已填 `cloud1-d9go68a317d35c262`。
2. 建集合权限改成「所有用户可读、仅创建者可读写」：已有 `users` / `events` / `registrations`；新加 `clubs` / `club_members` / `players` / `galleries` / `venues` / `match_records`（`seedMock` 也会 `createCollection`，但仍要改权限，否则客户端读不到）。
3. 开发者工具打开仓库根目录，把 `cloudfunctions/` 五个函数右键「上传并部署：云端安装依赖」（尤其是更新过的 `seedMock`、`login` 和新的 `clubAction`）。
4. 榜单头像、俱乐部 logo、球场照改为云存储网络图。
5. 报名收费：企业主体认证 + 微信支付商户号。个人 AppID 做不了支付。

---

## 设计债（可后补）

| 事项 | 说明 |
| --- | --- |
| 领奖台金圈 | 仍烤在台座切图里；「我的」V5 已改圆形封面头像 |
| 字号 | 卡片信息 17rpx ≈ 8.5pt，已按设计实现，真机看完再决定是否放大 |
| 主包体积 | 代码质量建议主包 &lt; 1.5MB（上传硬限 2MB）。`assets/` 已明显变大，可再压图或给相册/海报导包。脚本草稿：`tools/shrink-mp-assets.cjs`（**还没跑过**） |
| 文档数字 | README / AGENTS 里写的 assets 体积已过时，以开发者工具「代码质量」为准 |
| 草稿里的榜单/相册/日历/俱乐部列表 | 画板标了 Prototype，视觉接近旧终稿，沿用原页没有另写一套 |

---

## 明确不做

- 对战、约战匹配、VS、H2H
- 赛事详情里的「组队」做成约战（组队 Tab 只做赛事招募）

---

## 建议顺序

1. 上传 `seedMock`、`clubAction`、`login`，编译预览后看控制台是否出现新集合。
2. 测：俱乐部列表 → 申请加入 → 「我的俱乐部」；球员榜 / 俱乐部榜；相册；店铺；参赛记录。
3. 体验版预览稳定后，再考虑企业主体接支付。
