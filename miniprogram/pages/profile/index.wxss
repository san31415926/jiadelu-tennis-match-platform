/**
 * ============================================================================
 * 我的页样式
 * ============================================================================
 * 数值来自 Figma node 10:212 及其子节点。
 *
 * 【这一页为什么大量用绝对定位】
 * 球员卡头部里的头像、昵称、评分胶囊、身价、积分、进度条是"错落摆放"的，
 * 不是规整的行列结构（比如积分的标签在 top:200，身价的标签在 top:195，
 * 差了 5rpx）。这种手工微调的排版用 flex 很难精确复现，用绝对定位照抄
 * 设计稿坐标最准也最好核对。
 *
 * 【想整体调整头部布局】
 * 所有元素的 top 值都是相对 .hero__inner（368rpx 高）的顶部算的。
 * 想让整块内容下移，不要逐个改 top，改 .hero__inner 的 padding-top，
 * 或者在 wxml 里给 .hero 的 padding-top 加数值。
 *
 * 【下半部分的菜单是流式布局】
 * 菜单用的是普通文档流，所以往 mock/profile.ts 里加菜单项会自动变长，
 * 不需要改任何高度。球员卡和菜单中间那条 VIP 横幅也是流式的。
 *
 * 【头像是金框组件，不是一张合成图】
 * 头部头像用 components/gold-avatar，金圈和相机是独立切图。
 * 想换示例头像改 mock/profile.ts 的 avatar 字段。
 */

.profile {
  display: flex;
  flex-direction: column;
  gap: 17rpx;
  min-height: 100vh;
  background: #f7faec;
}

/* ---------- 球员卡头部 ---------- */

.hero {
  position: relative;
  width: 100%;
  background: linear-gradient(180deg, #abe514 0%, #c7f038 62%, #f0fab8 100%);
  box-shadow: 0 12rpx 24rpx rgba(41, 56, 20, 0.16);
}

.hero__inner {
  position: relative;
  width: 100%;
  height: 368rpx;
  overflow: hidden;
}

/* 波浪路径取自 node 10:214，贴在头部下缘 */
.hero__wave {
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 105rpx;
  background-image: url("data:image/svg+xml;charset=utf8,%3Csvg xmlns='http://www.w3.org/2000/svg' width='750' height='105.289' viewBox='0 0 750 105.289' preserveAspectRatio='none'%3E%3Cpath d='M0 51.3173C92 15.0413 166 78.7455 263 62.8194C343 49.5477 373 24.7739 448 49.5477C546 80.515 628 31.8521 750 0V105.289H0V51.3173Z' fill='%23FBFFF4'/%3E%3C/svg%3E");
  background-repeat: no-repeat;
  background-size: 100% 100%;
}

/* 标题在设计中的中心是 x=354，比屏幕正中略偏左。z-index 避免被金框/网球盖住 */
.hero__title {
  position: absolute;
  left: 174rpx;
  top: 20rpx;
  z-index: 3;
  width: 360rpx;
  height: 28rpx;
  text-align: center;
  font-size: 40rpx;
  font-weight: 700;
  line-height: 48rpx;
  color: #1c1f21;
}

/**
 * 金框头像组件的位置。组件自己是 207×197（含右下角相机），
 * 这里只负责放到头部里。数值沿用原先合成图的 left/top，
 * 避免昵称、胶囊和头像挤在一起。
 */
.hero__avatar {
  position: absolute;
  left: 32rpx;
  top: 12rpx;
  z-index: 1;
}

.hero__ball {
  position: absolute;
  left: 597rpx;
  top: 28rpx;
  z-index: 1;
  width: 133rpx;
  height: 116rpx;
}

/* 设计给昵称留了 99rpx（示例昵称只有一个字），长昵称截断显示 */
.hero__nickname {
  position: absolute;
  left: 276rpx;
  top: 138rpx;
  width: 99rpx;
  height: 42rpx;
  overflow: hidden;
  font-size: 34rpx;
  font-weight: 700;
  line-height: 42rpx;
  color: #1f2414;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.hero__nickname--guest {
  text-decoration: underline;
  color: #3d5c0a;
}

.hero__chip {
  position: absolute;
  top: 133rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 51rpx;
  border-radius: 16rpx;
  background: rgba(88, 153, 88, 0.6);
  font-weight: 600;
}

.hero__chip--rating {
  left: 377rpx;
  width: 105rpx;
  font-size: 24rpx;
  color: #ffffff;
}

.hero__chip--level {
  left: 508rpx;
  width: 109rpx;
  font-size: 32rpx;
  color: #3d452d;
}

.hero__label {
  position: absolute;
  height: 24rpx;
  font-size: 16rpx;
  line-height: 24rpx;
  color: #5c6e33;
}

.hero__label--value {
  left: 33rpx;
  top: 195rpx;
}

.hero__label--points {
  left: 467rpx;
  top: 200rpx;
}

.hero__number {
  position: absolute;
  top: 218rpx;
  height: 48rpx;
  font-size: 34rpx;
  font-weight: 700;
  line-height: 48rpx;
  color: #1f2414;
}

.hero__number--value {
  left: 61rpx;
}

.hero__number--points {
  left: 517rpx;
}

.hero__trend {
  position: absolute;
  left: 221rpx;
  top: 228rpx;
  height: 28rpx;
  font-size: 18rpx;
  font-weight: 600;
  line-height: 28rpx;
  color: #e4573a;
}

.hero__xp-hint {
  position: absolute;
  left: 32rpx;
  top: 292rpx;
  height: 24rpx;
  font-size: 24rpx;
  line-height: 24rpx;
  color: #5c6e33;
}

/**
 * 经验进度条。
 *
 * 由三层叠成：
 *   .hero__xp-track 灰色底槽，固定 690rpx 长
 *   .hero__xp-fill  绿色填充，宽度由 JS 按百分比算出来（见 index.ts）
 *   .hero__xp-ball  末端那个小网球，位置也是 JS 算的
 *
 * 两者 left 和 top 完全一样，所以填充是精确叠在底槽上的。
 * 改了 left/top 记得两个选择器一起改（它们写在同一条规则里就是为了不漏）。
 *
 * height: 8rpx 配 border-radius: 4rpx（正好一半）让两端是半圆。
 * 想让进度条更粗，两个值同时按比例调，比如 12rpx 配 6rpx。
 */
.hero__xp-track,
.hero__xp-fill {
  position: absolute;
  left: 32rpx;
  top: 330rpx;
  height: 8rpx;
  border-radius: 4rpx;
}

.hero__xp-track {
  width: 690rpx;
  background: #e5ebbf;
}

.hero__xp-fill {
  background: #80bf05;
}

/* node 10:229 是一个描白边的圆，用 CSS 绘制即可 */
.hero__xp-ball {
  position: absolute;
  top: 317rpx;
  width: 28rpx;
  height: 28rpx;
  box-sizing: border-box;
  border: 2rpx solid #ffffff;
  border-radius: 50%;
  background: #d1f038;
}

/* ---------- 会员横幅 ---------- */

/**
 * 插在球员卡和菜单中间。白卡片 + 金穗网球 VIP 标 + 青柠开通按钮。
 * 标是生图抠出来的透明 PNG：assets/icons/profile/vip-wheat-tennis.png
 * 想改文案改 index.wxml；想改按钮颜色改 .vip-bar__btn 的 background。
 */
.vip-bar {
  display: flex;
  align-items: center;
  height: 108rpx;
  margin: 0 20rpx;
  padding: 0 16rpx 0 12rpx;
  border-radius: 28rpx;
  background: #ffffff;
  box-shadow: 0 4rpx 10rpx rgba(38, 56, 20, 0.08);
}

.vip-bar__emblem {
  flex-shrink: 0;
  width: 88rpx;
  height: 81rpx;
}

.vip-bar__text {
  flex: 1;
  margin-left: 12rpx;
  overflow: hidden;
  font-size: 26rpx;
  font-weight: 700;
  color: #1f2114;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.vip-bar__btn {
  flex-shrink: 0;
  height: 52rpx;
  padding: 0 22rpx;
  border-radius: 26rpx;
  background: #83d414;
  color: #1f2114;
  font-size: 24rpx;
  font-weight: 700;
  line-height: 52rpx;
}

.menu {
  overflow: hidden;
  border: 1rpx solid rgba(219, 229, 194, 0.75);
  border-radius: 7rpx;
  background: #fcfcfb;
  box-shadow: 0 8rpx 10rpx rgba(38, 56, 20, 0.1);
}

/**
 * 菜单的一行。
 *
 * 【横向空间分配】
 * padding 16 + 图标 88 + gap 14 + 文字(自适应) + gap 14 + 箭头 30 + padding 16
 * 文字区用 flex: 1 占满剩余空间，所以菜单文字再长也不会挤掉箭头。
 *
 * 【分隔线】
 * 用 border-bottom 而不是单独的 view，这样加减菜单项时分隔线自动跟着，
 * 不用手动维护。最后一项也有分隔线，和设计稿一致。
 */
.menu__item {
  position: relative;
  display: flex;
  gap: 14rpx;
  align-items: center;
  height: 104rpx;
  padding: 0 16rpx;
  border-bottom: 1rpx solid rgba(229, 235, 219, 0.75);
  background: var(--card-bg);
}

.menu__item--pressed {
  background: #f6f9ee;
}

/**
 * 「分享好友」必须用 button 的 open-type="share"，微信不让 JS 直接调转发。
 * 原生 button 自带边框、居中、内边距，直接当菜单行会缩成一小块。
 * 所以这一行的样子仍用普通 view，button 做成铺满的透明热区。
 */
.menu__share-hit {
  position: absolute;
  left: 0;
  top: 0;
  z-index: 1;
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  border: none;
  border-radius: 0;
  background: transparent;
}

.menu__share-hit::after {
  border: none;
}

.menu__icon {
  flex-shrink: 0;
  width: 88rpx;
  height: 88rpx;
}

/**
 * 水平翻转图标。
 *
 * 设计里「商务合作」的握手和「关于我们」的头像做了左右镜像，但导出的图片
 * 是翻转前的原图，所以用 CSS 补回来。哪些项需要翻转由 mock/profile.ts 里
 * 每项的 mirrored 字段控制。
 *
 * scaleX(-1) 就是沿垂直轴翻转；scaleY(-1) 是上下翻转。
 */
.menu__icon--mirrored {
  transform: scaleX(-1);
}

.menu__label {
  flex: 1;
  font-size: 36rpx;
  font-weight: 500;
  color: #57772f;
}

.menu__arrow {
  flex-shrink: 0;
  width: 30rpx;
  text-align: center;
  font-size: 34rpx;
  line-height: 42rpx;
  color: #668c14;
}
