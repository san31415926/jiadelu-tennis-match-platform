/**
 * ============================================================================
 * 超级杯页样式
 * ============================================================================
 * 数值来自 Figma node 1:307 及其子节点。
 *
 * 【和首页宫格的关键区别】
 * 首页七宫格的图标是逐个手调位置尺寸的（每项带 iconStyle），
 * 超级杯八宫格在设计里是规整的两行四列，图标统一 110×105，
 * 所以这里用 flex 居中统一控制，不需要逐项坐标——写法比首页简单得多。
 *
 * 结构是：容器（纵向）→ 两行（横向）→ 每格（纵向：图标在上、文字在下）
 *
 * 【想改成一行三个（共两行剩两个）】
 * 改 pages/super-cup/index.ts 里 toRows(SUPER_CUP_FEATURES) 的第二个参数
 * （默认 4），页面会自动重排。但格子宽度 158rpx 也要相应调大，
 * 否则一行会留很多空白。
 */

/* 宫格容器渐变与首页不同：自上而下 #fcfff8 -> #f0ffd3 */
.feature-grid {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
  height: var(--grid-height);
  padding: 12rpx 0;
  overflow: hidden;
  background: linear-gradient(180deg, #fcfff8 0%, #f0ffd3 100%);
}

.feature-grid__row {
  display: flex;
  gap: 16rpx;
  height: 158rpx;
  padding: 0 32rpx 0 38rpx;
  overflow: hidden;
}

.feature {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  align-items: center;
  justify-content: center;
  width: 158rpx;
  height: 154rpx;
  overflow: hidden;
}

.feature--pressed {
  opacity: 0.75;
}

.feature__icon {
  flex-shrink: 0;
  width: 110rpx;
  height: 105rpx;
}

.feature__label {
  font-size: 20rpx;
  font-weight: 600;
  text-align: center;
  color: var(--text-grid-label);
}

.feed {
  display: flex;
  flex-direction: column;
  padding: 16rpx 26rpx 24rpx;
  border-radius: var(--radius-feed);
  background: var(--feed-gradient);
}

.feed__card + .feed__card {
  margin-top: 16rpx;
}

.feed__empty {
  padding: 72rpx 0;
  text-align: center;
  font-size: 22rpx;
  color: var(--text-muted);
}
