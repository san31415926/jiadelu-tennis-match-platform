---
description: 本项目的注释风格：面向非专业开发者的调参说明
globs: miniprogram/**
alwaysApply: false
---

# 注释风格

项目所有者不是专业开发者，注释是他理解和调整代码的主要途径。**这是项目所有者明确
要求的风格，不要按"好代码不需要注释"的通行原则去精简它。**

每个文件顶部要有说明块，回答三个问题：这是干什么的、改它会看到什么效果、
有什么配套约束不能漏。用中文写。

## 关键数值必须说明来源和调法

```wxss
/* ❌ 只描述现状，读者不知道能不能改、怎么改 */
.tabbar__halo { left: 85rpx; width: 81rpx; }

/* ✅ 说明来源、算法、以及改动的连带影响 */
/**
 * 选中态的椭圆光晕（Figma node 1:297）。
 * 81×76 的椭圆，left: 85rpx 让它在 250rpx 宽的格子里水平居中
 * （(250 - 81) ÷ 2 ≈ 85）。所以改了格子宽度或光晕尺寸，left 要重新算。
 */
```

## 反直觉的写法必须解释为什么

比如 `[...pool].sort()` 里的展开运算符看着多余，实际是防止 sort 污染原数组；
`overflow: hidden` 裁掉图标是设计稿的刻意效果，不是 bug。这类地方不写注释，
后来者会"顺手优化"掉。

## 说明跨文件的配套关系

改一处常要动两三个文件。注释里要点明另一处在哪，例如：

> 改这个记得同步改 `app.wxss` 里 `.header__wave` 那段 SVG 里的 fill 值，
> 否则波浪和页面之间会出现色差线。

## `.json` 文件不能写注释

标准 JSON 不允许注释，加了会编译报错。`app.json`、`project.config.json`、
页面 json 的说明统一写在 `miniprogram/工程说明.md`，不要试图在 json 里加。
